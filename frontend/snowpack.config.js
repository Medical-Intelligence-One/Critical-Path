// Snowpack Configuration File
// See all supported options: https://www.snowpack.dev/reference/configuration

// SMS: This was generated from `snowpack init`. At this time all of the
// defaults are fine. We will customize this as our frontend build starts to
// demand that level of complexity/investment.

/** @type {import("snowpack").SnowpackUserConfig } */
module.exports = {
  mount: {
    /* ... */
  },
  plugins: [
  ],
  packageOptions: {
    /* ... */
  },
  devOptions: {
    /* ... */
  },
  buildOptions: {
    /* ... */
  },
};
